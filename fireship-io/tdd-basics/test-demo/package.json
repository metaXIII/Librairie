{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "test": "jest --watchAll --verbose",
    "test-coverage": "jest --watchAll --verbose --coverage",
    "e2e": "cypress open"
  },
  "devDependencies": {
    "@types/jest": "^29.0.0",
    "vite": "^4.0.0"
  },
  "dependencies": {
    "jest": "^29.4.3"
  }
}
